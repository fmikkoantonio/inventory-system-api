version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: inventory-mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: inventory-system
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
